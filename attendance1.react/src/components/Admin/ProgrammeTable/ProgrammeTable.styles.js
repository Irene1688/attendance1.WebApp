import { alpha } from '@mui/material/styles';

export const styles = (theme) => ({
  actionButton: {
    display: 'flex',
    gap: theme.spacing(1)
  },

  countCell: {
    fontWeight: 500,
    color: theme.palette.primary.main
  }
}); 